# NT Cauldron

NetTantra's installer for local development infrastructure: MySQL, PostgreSQL, Redis, and Adminer via Docker Compose.

## Setup

```sh
curl -fsSL https://hsclr.net/nt-cauldron | bash
```

Or with wget:

```sh
wget -qO- https://hsclr.net/nt-cauldron | bash
```
